/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'static.pixieset.com',
            'images-pw.pixieset.com'
        ],
    },
};

export default nextConfig;
